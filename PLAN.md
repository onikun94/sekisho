# Sekisho（関所）— プロダクト計画書

> 本書は `CONCEPT.md`（コンセプトの核）と `docs/research-synthesis.md`（市場・技術調査）を前提に、実行計画に落としたもの。コンセプトの語彙・設計原則・課金方針とは矛盾させない。数値の根拠は原則として research-synthesis.md を参照し、本書内で新たに決める数値（定足数・休眠日数など）は「要検証」と明記する。
>
> **2026-08-17 方針更新:** ソーシャルMVPは招待制の固定五人組ではなく、8〜20人程度を毎週自動編成する匿名週間リーグとする。個別DM、グループチャット、自由入力プロフィール、MVPの応援スタンプは実装しない。本書内の旧「五人組」「招待コード」「投票」記述と矛盾する場合は、`docs/cloudflare-implementation-plan.md` と `CONCEPT.md` の最新方針を優先する。

## 1. ゴールと非ゴール

### 1.1 ゴール

- 「誘惑に勝てない意志力を、仕組みと他人の目で外部化したい」という未解決ジョブに対し、日本市場に存在しない「見知らぬ人との相互アカウンタビリティ × earn-access ロック」の受け皿を提供する。
- コアループ（関所→務め→通行手形→自動共有→番付）を PoC → MVP → v1 → v2 の順で段階的に検証し、各段階で仮説が崩れた場合に引き返せる計画にする。
- コアループは永久無料を維持したまま、L2/L3課金で個人開発として持続可能な収益（月商ベンチマークは本書 8章参照）を作る。

### 1.2 非ゴール（CONCEPT.md「作らないもの」を反映）

- **自由入力の DM・チャットは作らない（初期）** — モデレーション負荷と Guideline 1.2 対応コストを避けるため、感情表現は定型スタンプに限定する。
- **罰金・デポジット型の金銭ペナルティは作らない（初期）** — Pact・stickK の破綻事例（返金命令・収益未達）を踏まえ、L4フェーズまで着手しない。個人開発でチャージバック運用と消費者保護対応を抱えるリスクを負わない。
- **正確な利用時間（分数）の共有は作らない** — 技術的に不可能（DeviceActivityReport extension はネットワーク禁止・App Group 経由でも extension→アプリ方向の受け渡し不可）であり、思想としても「見せるのは分数でなく約束の成否」という原則に反する。
- **他人の端末を直接ロックする機能は作らない** — 該当 API が存在せず、ADPLA 3.3.3(P) にも抵触する。ロックは常に自分の端末が自分に掛ける。
- **他社アプリの達成状況を条件にする機能は作らない** — 公開 API が存在しない（例: 他アプリのレッスン完了を検知する手段はない）。

## 2. ペルソナとジョブ

**プライマリーペルソナ**: 22歳、TikTok に1日3時間を溶かし、資格勉強・受験勉強が続かない。みんチャレ的な「励まし」は照れくさくて続かず、Opal 的な自己ロックは自分で解除してしまう。

**ジョブ**: 「誘惑に勝てない自分の意志力を、仕組みと他人の目で外部化したい」。みんチャレが解くのは孤独（一人だと続かない）、本アプリが解くのは意志力（一人だと破ってしまう）。励ましが欲しい人ではなく、**縛ってほしい人**の受け皿であり、日本製の同種プロダクトは調査時点で確認できていない。

**Non-target（意図的に狙わないユーザー）**: 承認・励ましを求めて写真報告を続けられる人（みんチャレの主戦場）、他人からの監視自体に強い抵抗を持つ人（日本の相互監視への抵抗感は研究記事でも指摘されており、無理に説得しない）。

## 3. フェーズ計画

各フェーズは「前フェーズの完了条件を満たしてから着手する」ゲートとして機能させる。数値目標は初期仮説であり、実測値でフェーズ内に調整してよい（要検証）。

### Phase 0: PoC — シールド + earn-access をソロで検証

**目的**: ソーシャル機能に着手する前に、コアループの土台（FamilyControls / DeviceActivity / ManagedSettings / ShieldConfiguration / ShieldAction extension）が実機で成立するかを開発者本人1人で検証する。ネットワーク機能・アカウント機能は作らない。

**完了条件**:
- シールド画面のボタン（最大2つ）から自アプリへ遷移する導線が実機で機能することを確認済み（通知経由ワークアラウンドの実機検証。詳細は9章のチェックリスト）。
- アプリ内フォーカスセッション達成 → `ManagedSettingsStore` によるロック解除が、複数日間連続して安定動作することを確認済み（誤解除・解除漏れなし）。
- `FamilyControls` distribution entitlement を申請済み（MVP の TestFlight 配布に間に合うよう、PoC 開始と同時に申請する）。
- 開発者本人が最低7〜14日間、実際にロックのある生活でドッグフーディングし、ゼロ・レポーティングのコアループが「続けたいと思えるか」を主観評価済み。

### Phase 1: MVP — 招待コード五人組

**目的**: 匿名マッチングという審査リスクの高い機能に進む前に、招待コードによる「友人五人組」でソーシャルレイヤー（達成の自動共有・手形申請・投票・番付・ストリーク）を検証する。招待コード方式は審査リスクが低い基本線であり、最初の口コミ導線でもある。

**完了条件**:
- 招待コードで組成された友人五人組が最低3組、2週間以上継続稼働している。
- 手形申請 → push 通知 → 投票 → 結果確定のフローが実機で完結している。
- 達成の自動共有（ゼロ・レポーティング）が実装され、ユーザーが「報告する」操作を一度も行わずにコアループが回っている。
- クラッシュフリー率等の品質基準を満たし、TestFlight 外部テスターでの致命的不具合報告がない状態。

### Phase 2: v1 — 匿名マッチング + 課金（L2/L3）

**目的**: 見知らぬ人同士のマッチングという本プロダクトの差別化の芯を投入し、同時にマネタイズを開始する。Guideline 1.2（通報・ブロック）への対応が前提条件。

**完了条件**:
- マッチング軸（ターゲットアプリ × 目標強度 × 活動時間帯）で五人組が自動組成され、手形申請への応答レイテンシの計測が始まっている。
- 通報・ブロック機能が実装され、App Store 審査を通過している。
- L2 Pro（サブスク）・L3 消耗（緊急手形）が実装され、7日トライアルの課金導線を含めてリリース済み。
- 7章の KPI（コアループ完遂率・D1/D7/D30・trial→paid・install→paid）を継続計測するダッシュボードが稼働している。

### Phase 3: v2 — グローバル展開

**目的**: 日本語版で実証したコアループとユニットエコノミクスを英語圏に展開する。#lockedin トレンドのような追い風があるうちに、TikTok マーケの本エンジン（UGC 風有料広告）を回す土台を作る。

**完了条件**:
- 英語ローカライズ（コピー・価格帯 $5.99/$39.99/$79.99 想定）が完了している。
- v1 の主要 KPI が一定水準（要検証: 具体的な閾値は v1 実測後に設定）に達しており、グローバル展開の投資判断が下せる状態。
- L4（Forfeit 型デポジット・BtoB）の検討着手可否を判断できるだけのユーザーデータが蓄積している。

## 4. 機能仕様

### 4.1 コミット（務め）の種類と判定方法

優先順位は research-synthesis.md 5.5節の堅牢性評価に従う。

1. **アプリ内フォーカスセッション（最優先・最堅牢）**: タイマー形式の集中セッション。バックグラウンド遷移を検知して中断とみなす。判定がアプリ内で完結するバイナリ条件のため、threshold 通知の遅延・早期発火バグに依存しない。
2. **写真証拠**: 務めの証拠写真を五人組に共有し、メンバーが判定役になる（人間の承認が入る点でみんチャレに近づくため、義務ではなく選択肢の一つに留める。CONCEPT.md 設計原則1）。
3. **HealthKit（歩数・運動）**: 端末が自動計測する客観データ。判定は容易だが、対象アプリの誘惑（TikTok等）と直接の関連が薄いユーザーには訴求力が弱い。
4. **他アプリのポジティブ利用 threshold**: 精度が最も低い（DeviceActivityMonitor の threshold 通知に依存するため）。優先度は最下位とし、補助シグナルとしてのみ扱う。

### 4.2 関所（ロック）のスケジュール

- 基本形は「設定時刻（例: 朝6時）に対象アプリ群が封鎖 → 務め達成まで解除されない」1日1回のスケジュール。
- 時間予算型（1日◯分まで自由に使える）はオプションとして後続フェーズで検討する。時間予算の実現には threshold 通知の精度問題が絡むため v1 以降、精度が実用に耐えると PoC/MVP で確認できてから着手する。
- Strict モード（L2 Pro）は緊急手形の使用そのものを無効化する設定。関所のスケジュール自体は変えず、「抜け道を自分に禁じる」機能として位置づける。

### 4.3 通行手形の発行 / 緊急手形 / 投票フロー

- **通行手形の発行**: 務めの達成をデバイスが検知した瞬間に自動発行される。ユーザーによる「報告」操作は存在しない（ゼロ・レポーティング）。
- **緊急手形**: 月1枚無料 + 追加購入（¥160〜320想定）、月3枚が上限。使用すると五人組に通知される。上限の存在自体が「社会契約は金で買えない」という製品の誠実さの証明であり、Strict モードの有料価値と整合する。
- **免除申請 → 投票**: 「今日は無理」を選ぶと五人組に免除申請が飛び、メンバーが投票（賛成/反対の定型スタンプ）で可否を決める。定足数・タイムアウトの設計は 4.4 節参照。

### 4.4 五人組マッチング（軸・定足数・休眠→補充）

- **単位**: 4〜6人（目安5人）の匿名スクワッド。1:1 にしない理由は、見知らぬ人の離脱・無反応を定足数投票で吸収するため（1人不在でも回る）。
- **マッチング軸**: ターゲットアプリ × 目標強度 × 活動時間帯。
  - 目標強度は、コミットの時間予算・頻度・Strict モード有無から算出する簡易ティア（Light / Standard / Strict）を想定（要検証）。本気度が近い相手同士を組ませることで、片方だけが緩む離脱を防ぐ。
  - 活動時間帯は嗜好ではなく機能要件——手形申請への応答レイテンシを下げるには、起きている時間が重なる相手が必要。
- **投票の成立条件（定足数）**: 全員一致ではなく「投票に参加したメンバー」を母数に多数決で判定する案を初期仮説とする。目安として、賛成または反対のいずれかが2票に達した時点で結果を確定し、一定時間（例: 12時間）経過しても2票に満たない場合は申請者救済のためタイムアウト自動承認とする（**要検証**: 閾値はβ期間中にA/Bで調整）。
- **休眠 → 補充**: 一定期間（例: 7日間、要検証）達成率0%かつ無反応のメンバーを「休眠」フラグに切り替え、さらに一定期間（例: 追加7日、要検証）経過で補充プールから自動アサインする。「追放」の語彙は使わず、休眠メンバー本人のストリークは壊さない（CONCEPT.md 設計原則・みんチャレの強制退出ショックの教訓）。

### 4.5 番付とストリーク

- **番付**: 週次リーダーボード。スコアは達成率ベース（務めを果たした日数 / 対象日数）を基本とし、五人組内ランキングを主、グローバルランキングは後続フェーズで検討する。
- **ストリーク**: 連続達成日数。L3 のストリーク保険（フリーズ、Duolingo型）で1日分の途切れを保護できる。保険の発動はペナルティではなく保護なので、緊急手形と異なり五人組への通知は不要（CONCEPT.md 課金方針）。

### 4.6 通報・ブロック（Guideline 1.2 対応）

- マッチング機能（v1以降）には Apple App Review Guideline 1.2 の UGC 要件として通報・ブロックの実装が必須。
- 通報カテゴリ（例: 嫌がらせ、スパム、なりすまし、不適切な写真証拠）を定型選択式にし、自由記述は理由欄程度に限定してモデレーション負荷を抑える。
- ブロックすると当該ユーザーとは以後マッチングされない。五人組内でブロックが発生した場合は 4.4 節の休眠→補充と同じ経路で補充する。
- 初期はフリーテキスト DM を作らないため、通報対象になり得る自由入力面はプロフィールと写真証拠のみに絞られる。

## 5. 技術アーキテクチャ

### 5.1 フロントエンド構成

- **UI**: SwiftUI（iOS ネイティブ、Dynamic Type / SF Symbols 前提）。
- **権限・選択**: `FamilyControls`（`.individual` 認可。家族共有前提の `.child` は使わない — 対等な成人同士のコミットメント契約という設計思想と一致）。対象アプリの選択は `FamilyActivityPicker`。
- **計測**: `DeviceActivity` / `DeviceActivityMonitor` で threshold ベースの補助シグナルを扱う（4.1節の通り、コア判定には使わない）。
- **ロック実行**: `ManagedSettings` / `ManagedSettingsStore` で対象アプリの制限を自分の端末に対して適用・解除する。
- **シールド外観**: `ShieldConfiguration` extension でテンプレート差し替え（背景ぼかし・アイコン・タイトル・サブタイトル・ボタン最大2つ）。自由レイアウトは不可。
- **シールド操作**: `ShieldAction` extension でボタン押下を処理する。シールドのボタンから自アプリを直接開くことはできないため、通知経由の遷移導線を PoC で検証する（9章）。

### 5.2 バックエンド比較と推奨

サーバーに送るデータは**達成フラグ・ロック状態・手形申請のみ**という制約（CONCEPT.md 技術制約ダイジェスト）を守れることを前提に、匿名認証・リアルタイム同期・push・投票（サーバーサイドロジック）の4観点で比較する。

| 観点 | Supabase | Firebase | CloudKit |
|---|---|---|---|
| 匿名認証 | ネイティブ対応（`signInAnonymously`） | ネイティブ対応（Anonymous Auth） | iCloud アカウントに暗黙依存。アプリ側の匿名ハンドルとは別に iCloud サインインが必須になる |
| リアルタイム同期 | Postgres の変更をWebSocketで購読（五人組の投票状況・ロック状態に向く） | Firestore のリアルタイムリスナー（実績豊富） | CKSubscription はあるが、投票のような多者間の集計状態向けの設計ではない |
| push | ネイティブ機能なし。Edge Function から APNs を直接叩く実装が必要 | FCM が APNs をラップ（実装は容易だが本質的な手間はAPNs証明書管理でSupabaseと大差ない） | CKSubscription + APNs が最も簡単 |
| 投票・マッチング等のサーバーサイドロジック | Edge Functions（Deno/TS）+ `pg_cron` で定足数集計・マッチング・休眠検知の定期実行を賄える | Cloud Functions + Cloud Scheduler で同等のことができるが、定期実行には Blaze（従量課金）プランへの切り替えが必須 | **サーバーレス実行環境がない**。CloudKit Web Services 経由で結局自前サーバーを立てる必要があり、"バックエンド運用を減らす"というCloudKitの利点が失われる |
| データモデルとの相性 | 五人組メンバーシップ・投票・番付は関係が明確なリレーショナルデータであり、Postgres の JOIN/GROUP BY で番付集計・投票集計が素直に書ける | NoSQL（Firestore）でも実装可能だが、多対多関係（メンバーシップ・投票）の非正規化設計が必要になり設計コストが上がる | 匿名の見知らぬ人同士のマッチングは CKShare（招待前提の共有モデル）と相性が悪く、公開データベースでの疑似実装は前例が薄い |
| プライバシー制約の担保 | Row Level Security で「サーバーには達成フラグ等のみ」をDB層で強制できる | セキュリティルールで同等のことは可能 | Apple のエコシステム内で完結する点は訴求力が高い |
| コスト・個人開発適性 | Pro プラン定額（要検証: 執筆時点の料金は変動するため契約前に要確認）で予算化しやすく、OSSで自己ホストへの移行余地もある（ベンダーロックイン低減） | 無料枠は広いが Cloud Functions 利用で従量課金化しやすい | 想定内利用なら実質無料。ただし前述の通りサーバーレス実行環境がないため、結局は別途サーバー費用がかかる可能性がある |

**推奨: Supabase。**

理由は次の3点に集約される。

1. 五人組・メンバーシップ・投票・番付はいずれも関係が明確なデータであり、Postgres のリレーショナルモデルと相性がよい。
2. Edge Functions + `pg_cron` により、投票の定足数集計・マッチングアルゴリズム・休眠ユーザーの検知と補充という「サーバーサイドロジックが必須の機能」を、追加のサーバー運用なしに実装できる。CloudKit はこの点が弱く、結局自前サーバーが要るなら CloudKit を選ぶ理由が薄れる。
3. Row Level Security により「サーバーに送るのは達成フラグ等のみ」という CONCEPT.md の制約を DB 層で機械的に強制でき、個人開発者が一人で運用してもプライバシー設計の破綻リスクを抑えられる。OSS で自己ホストへの移行余地があることも、長期コミットする個人開発者にとってベンダーロックイン回避になる。

push 通知だけは Supabase 単独ではネイティブ機能がなく、APNs 連携を自前で実装する必要がある（Edge Function から APNs HTTP/2 API を叩く。p8キーの管理は一度実装すれば継続コストは小さい）。この点は Firebase の FCM の方が体感の実装コストは低いが、決定的な差ではないため上記3点を優先して Supabase を推奨する。**Firebase は次点の代替案として保持し、PoC〜MVP初期の実装スパイクで Edge Function のコールドスタート遅延（手形申請→投票の応答性に影響する）を実測した上で最終判断を確定させる（要検証）。**

## 6. データモデル素案（エンティティと関係のみ）

- **User（利用者）**: 匿名ハンドル・アバター・認証ID。1人が1つの Subscription、1つの StreakRecord を持つ。
- **Barrier（関所）**: 対象アプリの選択・ロックスケジュール・Strictモード設定。User に対して1対多（無料は1つ、Pro は複数）。
- **Commitment（務め）**: 種別（フォーカスセッション/写真/HealthKit/他アプリ threshold）と達成条件パラメータ。Barrier に対して1対多。
- **LockState（関所の日次状態）**: 当日のロック中/解除済みステータス。Barrier に対して1対多（日次スナップショット）。
- **PassIssuance（通行手形）**: 発行日時・有効期限・発行理由（達成/投票承認/緊急手形）。User と Barrier に紐づく。
- **EmergencyPass（緊急手形の消費履歴）**: 使用月・残数・無料枠消費か追加購入かのフラグ。User に対して1対多。
- **ExemptionRequest（免除申請）**: 対象日・理由・ステータス（審議中/承認/却下）。User と Squad に紐づく。
- **Vote（投票）**: ExemptionRequest に対する SquadMembership 単位の賛否とタイムスタンプ。ExemptionRequest に対して1対多。
- **Squad（五人組）**: マッチング軸のスナップショット（ターゲットアプリ×目標強度×活動時間帯）、定員（4〜6）、招待コード方式かマッチング方式かの区分。
- **SquadMembership（五人組の加入関係）**: User × Squad の中間エンティティ。ステータス（アクティブ/休眠/離脱）と加入日を持つ。
- **StreakRecord（ストリーク）**: User に対して1対1。連続日数とストリーク保険の使用履歴。
- **RankingEntry（番付）**: 週次期間・Squad内スコア・順位。Squad に対して1対多（週次）。
- **Report / Block（通報・ブロック）**: 通報者・被通報者・カテゴリ・ステータス。User 同士の多対多関係として表現される。
- **Subscription（課金プラン）**: プラン種別（L1/L2）、トライアル状態、課金基盤（StoreKit）のトランザクションID。User に対して1対1。

主要な関係の流れ: `User —(1:N)— SquadMembership —(N:1)— Squad —(1:N)— ExemptionRequest —(1:N)— Vote`、`User —(1:N)— Barrier —(1:N)— Commitment` / `Barrier —(1:N)— LockState`、`User —(1:1)— StreakRecord`、`User —(1:1)— Subscription`。

## 7. App Review / entitlement 対応計画

### 7.1 申請タイミング

`FamilyControls` distribution entitlement は個別審査で数日〜4.5週間かかる（research-synthesis.md）。PoC フェーズの開始と同時に申請を出し、MVP の TestFlight 配布時期から逆算してリードタイムを確保する。早期申請自体が「規約解釈がどこまで通るか」を検証する唯一の手段でもある。

### 7.2 申請文の方針

ADPLA 3.3.3(P) が禁じるのは「他の成人の端末管理」「利用データの本人・家族外への共有」である。申請文では次を明確にする。

1. ロックは常に本人の端末を本人自身に対してのみ適用し、他者の端末を遠隔操作する機能は存在しない。
2. 五人組メンバー間で共有するのはロック状態・達成フラグ・手形申請のみであり、正確な利用時間（分数）は一切送信・共有しない。
3. 対象は対等な成人同士の自発的なコミットメント契約であり、保護者と子のペアレンタルコントロール（`.child` 認可）ではなく `.individual` 認可を用いる。
4. 類似の設計（LockPact 等）が承認された実例があることを補足材料として示す（要検証: 申請時点での最新の審査傾向を再確認する）。

### 7.3 リジェクトリスクと代替案

- **リスク**: 匿名マッチング機能が Guideline 1.2（通報・ブロック）未実装のままだと審査で止まる可能性が高い。
- **対策**: MVP は規約解釈リスクの低い招待コード五人組（友人間）から先にリリースし、匿名マッチングと投票機能は通報・ブロックを実装した v1 で追加する段階的アプローチを取る。
- **entitlement 自体が却下された場合の代替案**:
  - ロック機能を持たない「務め+五人組の見える化（手動でのアプリ利用自己申告）」版までスコープダウンし、実績を積んでから再申請する。
  - 招待コード五人組限定でリリースし、マッチング機能を伴わない「対等な少人数の自発的契約」という説明材料を積み増した上で再申請する。

## 8. KPI

- **コアループ完遂率**: 対象日のうち、務めを果たして通行手形を得られた日数の割合。
- **D1 / D7 / D30 リテンション**: インストールまたは初回関所設定からの継続利用率。
- **五人組の応答レイテンシ**: 手形申請から投票の定足数到達までの時間（中央値）。マッチング軸の「活動時間帯」の妥当性を検証する指標として使う。
- **trial → paid 転換率**: ベンチマーク 37.7%（research-synthesis.md 5.7節、H&F調査）。
- **install → paid 転換率**: APAC 中央値 2.4%（同上）。上位四分位ではより高い水準が見込める。
- 旧価格での参考試算（research-synthesis.md）: 10万DL × APAC中央値2.4% = 有料2,400人 × 月480円 ≒ 月115万円。現在の価格方針は `docs/revenuecat-product-spec.md` を正とし、本試算は価格決定の根拠には使用しない。

## 9. リスクと緩和策

| リスク | 緩和策 |
|---|---|
| threshold 通知の遅延・早期発火バグ（iOS 26.2でも未修正） | コア判定はアプリ内フォーカスセッション（バイナリ条件）に置き、時間ベースのシグナルは補助にとどめる（4.1節） |
| entitlement 審査の長期化・却下 | PoC 開始と同時に早期申請し、招待コード先行リリースで代替経路を用意する（7章） |
| 幽霊部員（無反応メンバー）による離脱誘発 | 定足数投票 + 活動時間帯マッチング + 「退出」でなく「休眠→補充」（強制退出ショックの回避） |
| BeReal 型「罰と監視」の UX 離脱（日次開封率9%まで失速した前例） | 罰でなく回収の達成文法、番付・ストリーク・スタンプ等の正の報酬系を同格で設計 |
| デポジット/罰金型の規制・オペレーションリスク（Pact の返金命令、stickKの収益未達） | L4フェーズまで着手しない。着手する場合もForfeit方式・外部決済（Stripe）限定 |
| みんチャレ型の無料枠段階縮小への反感の再現 | コア機能は永久無料と明言し、ロードマップとして後から絞らない方針をコミットする |
| Guideline 1.2 UGC対応漏れによる審査リジェクト | マッチング機能と通報・ブロックをセットで実装し、フリーテキストDMは作らない |
| シールドボタン→自アプリ遷移のワークアラウンドが実機で機能しない技術リスク | PoC最優先の検証事項とし、通知タップ等の複数導線を用意する（10章） |
| マッチング流動性不足（五人組が組めない・待ち時間が長い） | 招待コード五人組を併設して初期流動性を代替し、活動時間帯マッチングで応答性を確保する |

## 10. PoC 検証チェックリスト

優先度順。1番目（シールドボタン→自アプリ誘導のワークアラウンド）が全体の技術的成立性を左右するため最優先で検証する。

1. **【最優先】シールド画面のボタン（最大2つ）から自アプリへの遷移導線が実機で機能するか**。シールドの `ShieldAction` から自アプリを直接開くことはできないため、ローカル通知を発火 → 通知タップでアプリを開く、等の複数パターンを実機で検証し、体感の遅延・成功率を記録する。
2. `FamilyActivityPicker` で対象アプリを選択 → `ManagedSettingsStore` によるシールドが実機で正しくかかることを確認する。
3. アプリ内フォーカスセッション達成 → ロック解除が同一デバイス上で複数日連続して安定動作するか（誤解除・解除漏れがないか）を確認する。
4. `DeviceActivityMonitor` の threshold 通知の遅延・早期発火の実機挙動を記録し、補助シグナルとして採用可能な精度かを判断する。
5. Strict モード相当（手形の無効化）を実装した場合でも、ユーザーが自分の意志で全て解除できてしまわないか、UX として矛盾がないかを確認する。
6. `FamilyControls` distribution entitlement の申請を提出し、レスポンス内容（承認/条件付き/却下）と所要日数を記録する。
7. 開発者本人が最低1〜2週間ドッグフーディングし、ゼロ・レポーティングのコアループが継続利用に値するかを主観評価する。
8. 写真証拠・HealthKit連携について、実装コストと判定精度を比較し、4.1節のタスク種別優先順位を実測ベースで確定する。
