# Sekisho（関所）— UI設計書

> 本書は `CONCEPT.md` の関所メタファー語彙（関所・通行手形・番人・五人組・番付）を UI 文言レベルまで一貫させることを前提に、画面構成とコンポーネント方針を定義する。実装（SwiftUI コード）には踏み込まない。UI寸法は Apple 公式 Human Interface Guidelines（HIG）で確認できた数値のみを根拠付きで記載し、確認できていない・設計判断に委ねる数値は「要検証」と明記する。

## 1. デザイン方向性

- **余白の中の小さな情景**: 生成りの単色背景を画面端まで連続させ、関所と砂時計など、その画面で伝える事実を担う小さな情景だけを置く。イラストの周囲にカード、背景矩形、影を作らず、画面の余白と絵の余白を一続きに扱う。PoC ホームでは主題を画面の 12〜35% 程度に抑え、操作や数値より強くしない。
- **手書き文字は静的な短文だけ画像化する**: 大見出しと主要ボタンは Yomogi（SIL Open Font License）で正確な日本語を透過 PNG に焼き込み、イラストと同じ手描きの温度に揃える。利用時間や件数など変化する値はネイティブテキストのままにし、画像化した文字には必ず同じ内容のアクセシビリティラベルを付ける。
- **キャラクターは小さな情景の主役として扱う**: 将来の番人キャラは画面を埋める装飾ではなく、一画面につき一つの行動・状態を伝える役として使う。Duolingo のフクロウのようなミーム化・スキン展開の余地は残す（キャラの最終決定は本書では行わない。9章参照）。
- **和モチーフはアクセントに留める**: 関所・鳥居・提灯を想起させる意匠（朱色のアクセントカラー、印判風のスタンプ表現、番付の縦書き的な数字組）を差し色・モチーフレベルで使い、UI構造自体は素直な iOS ネイティブレイアウトを崩さない。世界観の説明コストを凝った専用レイアウトではなく、語彙とビジュアルモチーフの一貫性で肩代わりする。
- **iOS ネイティブ**: SwiftUI 標準コンポーネント + SF Symbols + Dynamic Type 完全対応を前提にする。独自フォント・独自タブバー等のカスタムチューニングは初期は行わない。
- **静けさを保った明瞭さ**: 大きな数値と墨色の本文、朱のアクセントで情報の優先順位を作る。カードの塗り分けや強い影でコントラストを作らない。シールド画面はテンプレート制約の中でも同じ生成り・墨・朱を使い、スクリーンショットや録画で世界観が途切れないようにする。
- **PoC はライトテーマを基準にする**: 生成イラストと外側の色面を連続させるため、現段階は生成り背景のライトテーマに固定する。ダークテーマは、暗色の連続背景に合わせた専用アセットとコントラスト検証を用意してから追加する（要検証）。

## 2. 情報アーキテクチャ（タブ構成）

4タブ構成: **ホーム（関所）/ 五人組 / 番付 / 自分**。

- **ホーム（関所）**: 今日の関所の状態（ロック中/通行済み）と務めの入口。アプリを開いたときの既定タブ。
- **五人組**: メンバーの通行状況、手形申請の投票、休眠→補充の通知。
- **番付**: 週次リーダーボードとストリーク。
- **自分**: プロフィール（匿名ハンドル・アバター）、関所ルールと Strict モードの設定、課金管理、通報・ブロックの導線。

Apple の Tab Bars ガイドラインは「タブが少ないほど回遊しやすい」とし、iPadOS のカスタマイズ可能なタブでは「5つ以下を既定にする」ことを推奨している。4タブはこの上限に余裕を持たせた構成であり、将来的な機能追加（例: 通知タブ）の余地を残す設計とする。

## 3. 主要フロー

### 3.1 オンボーディング

1. **目標選択**: 「TikTokを我慢して勉強したい」等、達成したい目標のカテゴリを選ぶ。
2. **Screen Time 権限のプライミング画面 → 権限取得**: OS標準の許可ダイアログを表示する前に、「なぜこの許可が必要か」を関所メタファーで説明する自社画面を挟む（例:「関所を築くには、あなたの端末の通行手形を管理する権限が必要です」）。権限拒否時の離脱を減らすための緩衝材として機能させる。`FamilyActivityPicker` は認可済みでないと使えないため、このステップは必ず対象アプリ選択より前に置く。
3. **対象アプリ選択**（`FamilyActivityPicker`）: システム標準の選択画面を呼び出す。
4. **務め設定**: コミットの種別（フォーカスセッション優先）と達成条件（例: 1日25分）を設定する。
5. **五人組参加**: 招待コード入力（友人と組む）または「五人組を探す」（マッチング、v1以降）を選ぶ。MVPでは招待コードのみ。
6. **7日トライアルのソフトペイウォール**: 「コアループは永久無料」の原則を守るため、オンボーディング完了直後に硬い課金壁は出さない。Pro機能（複数五人組・Strictモード等）のプレビューと7日トライアル開始の案内をソフトに提示し、スキップ可能にする。

### 3.2 日次コアループ

設定時刻に対象アプリが関所（シールド）で封鎖される → 務めを実行 → 通行手形が自動発行される → 五人組に自動共有される（ユーザー操作なし）→（必要な場合のみ）手形申請へ分岐 → 週次で番付・ストリークに反映される。

### 3.3 手形申請 → 投票

申請モーダルで理由を選択・入力 → 五人組メンバーに push 通知 → 各メンバーが投票カードで賛成/反対の定型スタンプを送る → 定足数到達（PLAN.md 4.4節）で結果確定 → 申請者に結果を通知。

### 3.4 休眠 → 補充

無反応が続くメンバーを検知 → 五人組内表示が「休眠」ラベルに切り替わる → 補充枠がオープンし新メンバーが自動加入 → 既存メンバーには「〇〇さんが新しく仲間に加わりました。あなたのストリークは影響を受けません」という趣旨のコピーで、休眠メンバー本人にも「追放」の語感を与えないラベリングを徹底する。

## 4. 画面別仕様

### 4.1 ホーム（関所）

- **目的**: 今日の関所の状態を一目で把握させ、務めの実行に誘導する。
- **主要コンポーネント**: 関所ステータスカード（ロック中/通行済み）、務め開始ボタン、五人組の通行状況ミニビュー（例: アバター5つ、通行済みはスタンプ表示）、ストリーク表示。
- **状態バリエーション**: ロック中／務め実行中／通行済み（当日分）／緊急手形使用済み（Strictモード時はこの状態自体が発生しない）。
- **コピー例**: 「今日の関所、まだ開いてません」「通行手形、受け取りました」「五人組は今日、全員通行済み」。

### 4.2 務め実行（集中セッションタイマー）

- **目的**: フォーカスセッションの実行と離脱防止。
- **主要コンポーネント**: 大きな残り時間表示、中断ボタン（明確に「あきらめる」ことのコストを示す文言を添える）、完了時の通行手形発行アニメーション。
- **状態バリエーション**: 実行中／一時中断可否（中断可否は Strict モード設定に依存）／達成／未達（アプリ切り替え等で中断された場合）。
- **コピー例**: 「あと8分。関所はもうすぐ開きます」「ここで やめますか？ 手形はもらえません」。

### 4.3 五人組（メンバー状況・投票カード・スタンプ）

- **目的**: 仲間の通行状況を見える化し、手形申請の投票を行わせる。
- **主要コンポーネント**: メンバーリスト（アバター・ハンドル名・当日の通行状況）、投票カード（申請理由 + 賛成/反対ボタン）、定型スタンプ選択パネル。
- **状態バリエーション**: 全員通行済み／投票待ちの申請あり／休眠メンバーあり（補充中）／自分が投票済みで結果待ち。
- **コピー例**: 「〇〇さんから手形の申請が届いています」「あなたの一票で決まります」「休眠中のメンバーがいます。まもなく補充されます」。

### 4.4 番付

- **目的**: 週次の達成をランキング形式で可視化し、継続動機を作る。
- **主要コンポーネント**: 週次ランキングリスト（五人組内が既定、要検証: グローバル版は後続フェーズ）、ストリークバッジ、週の切り替えコントロール。
- **状態バリエーション**: 週の途中／週締め直後（結果発表演出）／自分が上位／自分が下位。
- **コピー例**: 「今週の番付、あなたは3番手」「週締めまであと2日」。

### 4.5 シールド画面

特別仕様は 5章にまとめる。

### 4.6 手形申請モーダル

- **目的**: 「今日は無理」を選んだ際の免除申請の入力と送信。
- **主要コンポーネント**: 理由の選択（定型 + 短い自由記述の理由欄）、送信ボタン、送信後の「五人組の返答を待っています」状態表示。
- **状態バリエーション**: 入力中／送信済み・投票待ち／承認/却下の結果表示。
- **コピー例**: 「今日はどうしても無理な理由を教えてください」「五人組に届きました。返事を待ちましょう」。

### 4.7 ペイウォール

- **目的**: 7日トライアルと L2 Pro への転換を、コア機能を痩せさせずに提示する。
- **主要コンポーネント**: Pro特典リスト（複数五人組・複数関所・Strictモード・全期間データ・番人スキン・優先マッチング）、価格表示（月額/年額/ライフタイム）、トライアル開始ボタン、「今は始めない」の明確な離脱導線。
- **状態バリエーション**: トライアル未開始／トライアル中（残日数表示）／トライアル終了直後（再訴求）／Pro契約済み（このモーダル自体が非表示）。
- **コピー例**: 「7日間、無料でStrictモードを試せます」「無料のままでも、関所と五人組は一生使えます」（コアは永久無料の明言を必ずどこかに残す）。

### 4.8 設定（関所ルール・Strictモード）

- **目的**: 関所のスケジュール・対象アプリの変更と、Strictモードの有効化。
- **主要コンポーネント**: 関所ごとの設定リスト、対象アプリ再選択（`FamilyActivityPicker`）、Strictモードのトグル（有効化時は緊急手形が使えなくなる旨の確認ダイアログ）、通報・ブロックへの導線、退会・データ削除導線。
- **状態バリエーション**: Strict OFF／Strict ON（緊急手形使用不可の明示）／複数関所（Pro）。
- **コピー例**: 「Strictモードを有効にすると、今月の緊急手形は使えなくなります。よろしいですか？」。

### 4.9 通報・ブロック

- **目的**: Guideline 1.2 対応。不快なメンバーへの対処導線を明確に用意する。
- **主要コンポーネント**: 通報カテゴリ選択（定型）、ブロック確認ダイアログ、ブロック後の五人組再編内容の説明。
- **状態バリエーション**: 通報送信済み／ブロック確定（相手には通知しない設計を基本とする。要検証: モデレーションポリシー確定時に再確認）。
- **コピー例**: 「このメンバーを通報しますか？」「ブロックすると、この五人組から離れ、新しい仲間が補充されます」。

## 5. シールド画面の特別仕様

`ShieldConfiguration` はテンプレート差し替えのみが可能で、自由レイアウトは組めない。設定できるのは背景ぼかし・アイコン・タイトル・サブタイトル・ボタン最大2つ（プライマリ/セカンダリ）に限られる。この制約の中で番人キャラと五人組の進捗を表現する。

### 5.1 制約内でのレイアウト方針

- **アイコン**: 番人キャラの顔・表情差分をここに置く。ロック直後は「通せんぼ」の表情、通行済みメンバーが多いほど「呆れ」「急かし」寄りの表情に差し替える案（要検証: 表情差分の実装コストと `ShieldConfigurationDataSource` の更新タイミングを PoC で検証）。
- **タイトル**: 関所メタファーの短い一言（例:「関所、開かず」）。
- **サブタイトル**: 五人組の進捗を差し込む可変テキスト（例:「五人組の3人はもう通行済み」）。
- **ボタン（最大2つ）**: プライマリ=「務めを果たす」（自アプリへの遷移導線。3章参照）、セカンダリ=「今日は無理」（手形申請へ）。

### 5.2 データ鮮度に関する技術的制約（要検証）

`ShieldConfigurationDataSource` を含む Screen Time 系 extension は実行環境が厳しく制限されており、シールド表示のたびにネットワーク越しの最新データを取得できる保証はない（DeviceActivityReport extension のネットワーク禁止は研究資料で確認済みだが、ShieldConfiguration 側の制約は本書執筆時点で未確認のため要検証）。安全側の設計として、「五人組の3人はもう通行済み」という表示は、push 通知で受け取った最新状態をメインアプリが App Group の共有領域に書き込み、シールド extension がそこから同期的に読む方式を前提に計画する。表示される人数は「直近の同期時点」のものであり、厳密なリアルタイムではないことをUI設計上の前提として持っておく。

### 5.3 コピー案（Duolingo的な急かしトーンの日本語版、5例以上）

| 状況 | コピー案 |
|---|---|
| 初回ロック時のデフォルト | 「関所、開かず。今日の務め、まだですよ？」 |
| 五人組との進捗差分がある時 | 「五人組の3人はもう通行済み。あなたが最後の一人。」 |
| シールドに再挑戦（アプリを開こうとし直した）時 | 「番人、見てます。」 |
| よそ見を戒めるトーン | 「よそ見は、手形を受け取ってから。」 |
| 軽いプレッシャー・ミーム狙い | 「サボり、バレてます。」 |
| ストリーク継続中の煽り | 「ここで途切れさせますか、その連続記録。」 |
| 時間帯が遅い場合 | 「今日はまだ、ここまで。関所は務め待ちです。」 |
| 世界観寄りのユーモア | 「関所破りは、ここでは通用しません。」 |

いずれも語彙は「関所」「通行手形」「番人」「五人組」に統一し、罰・監視の恐さではなく「ちょっと笑える緊張感」（CONCEPT.md 設計原則6）のトーンを保つ。

## 6. コンポーネント / 寸法

寸法は Apple 公式 HIG（Human Interface Guidelines）で確認できた値のみを根拠として記載する。

### 6.1 タップターゲット

- Apple の Buttons ガイドラインは「ボタンは最低 44×44pt のヒットリージョンを持つ必要がある」と明記している。
- Apple の Accessibility ガイドラインでも、iOS/iPadOS のコントロールサイズは「既定 44×44pt、最小 28×28pt」とされている。
- 本アプリでは投票ボタン・務め開始ボタン・タブバー項目など、すべての主要タップ要素で **44×44pt を既定の最小ヒットリージョン**とする。視覚的なアイコンサイズがそれより小さくても、ヒットリージョンは余白込みで44pt を確保する。

### 6.2 タイポグラフィ（Dynamic Type）

Apple の Typography ガイドラインが定義する標準テキストスタイルの既定（Large）サイズは以下の通り。本アプリの見出し・本文は原則としてこれらのシステムテキストスタイルをそのまま使用し、独自のポイント指定は行わない。

| テキストスタイル | ウェイト | サイズ（既定/Large） |
|---|---|---|
| Large Title | Regular | 34pt |
| Title 1 | Regular | 28pt |
| Title 2 | Regular | 22pt |
| Title 3 | Regular | 20pt |
| Headline | Semibold | 17pt |
| Body | Regular | 17pt |
| Callout | Regular | 16pt |
| Subheadline | Regular | 15pt |
| Footnote | Regular | 13pt |
| Caption 1 | Regular | 12pt |
| Caption 2 | Regular | 11pt |

用途の目安: 番付の順位数字やシールドのタイトルは Title 1〜Large Title、五人組リストの本文は Body、投票カードの注記や日時表示は Footnote/Caption。

### 6.3 タブバー

- SF Symbols を使用し、独自のビットマップアイコンは作らない（Apple ガイドラインが SF Symbols の使用を推奨しており、Dynamic Type や太字設定に自動追従するため）。
- タブ数は4（2章参照）。HIGが示す「5つ以下」の目安の範囲内。

### 6.4 コントラスト

- Apple の Accessibility ガイドラインは WCAG レベルAAを基準とし、「17pt以下のテキストは4.5:1、18pt以上または太字のテキストは3:1」のコントラスト比を求めている。
- シールド画面・番付画面はスクリーンショット映えを狙うため、上記の最低ラインではなく、可能な限り高いコントラスト（黒地に白、または生成りに墨色などはっきりした明度差）を初期値とする（具体的なカラーコードは本書では規定せず、8章の基準を満たす範囲でビジュアルデザイン時に確定。要検証）。

## 7. アクセシビリティ

- **Dynamic Type**: Apple のガイドラインに従い、少なくとも200%までの文字拡大に耐えるレイアウトとする。番付の順位バッジや五人組のアバター行など横並び要素は、拡大時に折り返し・省略（Truncation）ができるようレイアウトを組む。優先度の低い装飾テキスト（和モチーフの飾り文言等）から先に省略する方針。
- **VoiceOver**: 五人組の投票カード・アバター行は、画像・状態ラベル・ハンドル名をバラバラに読み上げさせず、「〇〇さん、本日通行済み」のように1要素にまとめたアクセシビリティラベルを付与する。シールド画面（extension側）もタイトル・サブタイトル・ボタンラベルがそのまま VoiceOver で読み上げられることを前提に、コピーは単体で意味が通る文にする。
- **コントラスト**: 6.4節の基準を満たすことに加え、ダークモードでの朱色アクセントは彩度・明度を単純反転せず、視認性を確保する側に調整する（具体値は要検証）。
- **色以外の状態表現**: 通行済み/未通行、承認/却下といった状態は色だけで区別せず、アイコン形状やテキストラベルを併用する（色覚多様性への配慮）。

## 8. 番人キャラの画面上の使い方

CONCEPT.md が挙げる3候補（狛犬/デフォルメ仁王/関守の狸）は本書では決め打ちしない。以下は「どの画面のどこに住むか」というポジショニングのみを定義する。

- **シールド画面**: 番人の「顔」がアイコンスロットの主役。表情差分で五人組の進捗や督促のトーンを表現する主戦場（5.1節）。
- **オンボーディング**: ガイド役として各ステップに寄り添い、Screen Time 権限のプライミング画面では説明役を務める。
- **ホーム（関所）画面**: 関所ステータスカードの隅にワンポイントで登場し、状態（ロック中/通行済み）に応じた小さな表情差分を出す程度に留め、主役はステータス情報自体に譲る。
- **手形申請モーダル**: 投票結果（承認/却下）に応じた表情差分で結果を演出する役。
- **番付**: 週締めの結果発表演出に「祝う/煽る」表情差分で登場する。
- **ペイウォール**: 番人スキン（課金アイテム）の実演役として、Proで着せ替え可能なスキンのプレビューをここで見せる。

いずれの画面でも番人はテンプレート制約（シールド）または既存レイアウトの一要素（アプリ内画面）として配置し、番人のために専用の大きなレイアウトスペースを新設しない方針とする。
